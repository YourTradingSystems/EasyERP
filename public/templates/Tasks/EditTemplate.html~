<div class="form-holder">
  <form method="post" action="" id="createTaskForm" class="form" data-id="<%=model._id%>">
	<div class="ui-dialog-titlebar ui-widget-header ui-corner-all ui-helper-clearfix"><span id="ui-id-2" class="ui-dialog-title"><%=model.project.projectShortDesc%></span><button class="ui-button ui-widget ui-state-default ui-corner-all ui-button-icon-only ui-dialog-titlebar-close" role="button" aria-disabled="false" title="close"><span class="ui-button-icon-primary ui-icon ui-icon-closethick"></span></button></div>
      <div class="formTitle">
		<h3 class="left"><%=model.project.projectShortDesc%></h3>
		<div class="clearfix"></div>
	  </div>
    <div class="half-block left withPaddingLeft">
      <div class="formTitle">
		<h3 class="left">Description</h3>
		<div class="clearfix"></div>
	  </div>
	  <div class="inner">
		<textarea type="text" name="description" placeholder="Add a Description" id="description" value="<%=model.description%>" required />
	  </div>
    </div>
    <div class="half-block left">

      <div class="formTitle">
		<h3 class="left">Extra Info</h3>
		<div class="clearfix"></div>
	  </div>
	  <div class="inner">
		<dl class="person-info">
		  <dt><label>Summary: </label></dt><dd><input type="text" name="summary" class="input-title blueback" id="summary" value="<%=model.summary%>" required /></dd>

		  <dt><label>Project</label></dt><dd>
			<select id="projectDd">
              <option value="0">Select...</option>
              <% projectsDdCollection.forEach(function(option){ %>
              <option value="<%= option._id %>" <%= (option._id == model.project.id) ? 'selected="selected"' : ''%>><%= option.projectName %></option>
              <% }); %>
			</select>
		  </dd>
		  <dt><label for="workflowDd">Task Workflow</label></dt><dd>
            <select id="workflowDd">
              <option value="<%=model.workflow.name%>">Select...</option>
              <% workflowsDdCollection.forEach(function(option){ %>
              <option value="<%= option._id %>" <%= (option.name == model.workflow.name) ? 'selected="selected"' : ''%>><%= option.name %> (<%= option.status %>)</option>
              <% }); %>
            </select>

		  </dd>

		  <dt><label>Assigned to</label></dt><dd>
			<select id="assignedTo">
              <option value="<%=model.assignedTo.id%>">Select...</option>
              <% accountsDdCollection.forEach(function(option){ %>
              <option value="<%= option._id %>" <%= (option._id == model.assignedTo.id) ? 'selected="selected"' : ''%>> <%= option.name.first %> <%= option.name.last %></option>
              <% }); %>
			</select>
		  </dd>


		  <dt><label for="deadline">Deadline </label></dt><dd><input class="blueback" type="text" name="deadline" id="deadline" value="<%= model.deadline %>" required /></dd>

		  <dt><label for="priority">Priority </label></dt><dd>
		    <select id="priority">
              <option value="<%=model.extrainfo.priority._id%>">Select...</option>
              <% priorityCollection.forEach(function(option){ %>
              <option value="<%= option._id %>" <%= (option.priority == model.extrainfo.priority) ? 'selected="selected"' : ''%>> <%= option.priority %></option>
              <% }); %>
            </select>
		  </dd>

		  <dt><label for="StartDate">Start Date </label></dt><dd><input class="extrainfo blueback" type="text" name="StartDate" id="StartDate" value="<%= model.extrainfo.StartDate %>" required /></dd>
		  <dt><label for="EndDate">End Date </label></dt><dd><input class="extrainfo blueback" type="text" name="EndDate" id="EndDate" value="<%= model.extrainfo.EndDate %>" required /></dd>

		  <dt><label for="customerDd">Customer</label></dt><dd>
            <select id="customerDd">
              <option value="<%=model.extrainfo.customer.id%>">Select...</option>
              <% customersDdCollection.forEach(function(option){ %>
              <option value="<%= option._id %>" <%= (option._id == model.extrainfo.customer.id) ? 'selected="selected"' : ''%>><%= option.name %> (<%= option.type %>)</option>
              <% }); %>
            </select>

		  </dd>
		  <dt><label for="logged">Logged</label></dt><dd>
			<input class="blueback" type="text" name="loged" id="logged" value="<%=model.logged%>"/>
		  </dd>
		  <dt><label for="estimated">Estimeted</label></dt><dd><input class="blueback" type="text" name="estimated" id="estimated" value="<%=model.estimated%>" /></dd>
		</dl>	  

      </div>
      <div class="clearfix"></div>
	</div>
    <div class="buttons">
	  <input type="submit" id="saveBtn" class="btn" value="Save"/>
	  <input type="button" id="cancelBtn" class="btn" value="Cancel"/>
	</div>
  </form>
</div>
